google-generativeai==0.8.3   # talks to Gemini
pdfplumber==0.11.4            # reads PDF resumes
python-docx==1.1.2            # reads Word resumes
python-dotenv==1.0.1          # loads your API key from .env
numpy==1.26.4                 # does the similarity math
pandas==2.2.3                 # writes the CSV output